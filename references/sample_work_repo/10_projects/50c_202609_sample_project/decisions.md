# decisions

判断と、その理由の記録。**contextflow が追記する。**

`python app/cf.py github export` を実行すると、その日の変化・判断が
次の書式で日付ごとに追記される。同じ行があれば重複しない。

## 2026-09-22

- 変化: 先方のAPI利用が不可と判明
- 判断: CSV連携方式に変更（理由: API利用不可のため）

## 2026-09-18

- 判断: 試作は既存の検証環境を流用（理由: 新規構築は時間がかかるため）
