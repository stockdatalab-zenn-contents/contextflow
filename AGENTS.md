# project instructions


## まず守ってほしい方針

- 英語で考えてから日本語で出力する
- 日本語は、敬語・丁寧語 ではなく、体言止め・用言止めにする
- 個々のタスクの実行は、サブエージェントを使う（10体までであれば許可なく使用して良い）
- 既存のファイル構成に合わせる
- 大きな変更の前に、方針を短く説明する
- サンプルは分かりやすさ優先で、複雑な抽象化を避ける
- 提案する際は、各案のメリット・デメリット・懸念点や注意点を挙げて、推奨案・根本対応案がどれか明記する
- 「確認したいポイント」を提示する直前に、推奨案をまとめる


## 使用モデルの選定

- GPT-5.6 SolまたはOpus 5など、作業開始時点で最も高性能なモデルがタスクを分解し、各タスクに最適なモデルとeffortレベルを割り当てる。
- 原則として、日常的な実装・修正・調査にはSonnet 5を使用する。
- 複雑な設計、難易度の高い問題、重要な判断・最終レビューにはOpus 5を使用する。
- モデル選択に関する実際の経験知（例：Sonnet 5では解決できず、Opus 5で解決できたケース）は、MODEL_SELECTION.mdに記録する。


## コーディング

- 不要な大規模リファクタリングはしない
- データ加工系の処理では型崩れ・NaN・ログ不足に注意する
- 時系列がある場合は、ファイル名に数値を振り状態と順序が分かるようにする
- 新規 Python スクリプトは `app/`配下の`source/`、設定は `source/config/`、テストは `tests/` に置く
- コメントは日本語で記載する
- **プログラムは可能な限り疎結合にし、使い回せるようにする**。
- **状態とロジックを分離し、コントラクト層（API/型）を厳密に、実装層は再生成可能に保つ**。
- 「.venv」環境やdocker環境の構築は、明確な指示が無い場合は、必要な資材のみ作成し、ユーザーに構築手順を提示して実施させること


## ドキュメント

- プロジェクトフォルダ直下に `README.md` を作成・更新し、使用方法・端的な処理概要を記載する
  - **記載順序は「セットアップ」「使い方」を冒頭に置く**。概要・処理の流れ・動作環境はその後に置く
  - 設定の全キー・内部設計などは README に書かず `docs/` 側へ分離する
- `docs/` 直下に その他のドキュメント（全体の設計・処理内容など）をyyyymmdd始まりで作成する
- ユーザーが判断・決定した事項は `DECISION.md` にマークダウン形式で、日時・判断内容・理由を記載する
- 修正履歴は `MODIFICATION_HISTORY.md` にマークダウン形式で記載する
- 上記の名前付きドキュメント（`DECISION.md` / `MODIFICATION_HISTORY.md`）は`docs/` 直下に置き、yyyymmdd 接頭辞は付けない


## サンプル参照

- `assets/` 内のファイルがあれば、それを参考にファイルを作成する
- ただし**コピーして使う**こと（直接 `import` は禁止）


## ファイル管理

- ファイルを更新したとき、または新しいワークフローで使わなくなったファイルは `99_old/` フォルダに移動する
- **`99_old/` フォルダ配下のファイルは、原則参照しない**

